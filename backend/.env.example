# Backend Environment Variables

# API Configuration
API_HOST=localhost
API_PORT=8000
API_DEBUG=True

# CORS Settings (frontend URLs that can access this API)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Optional: Database
DATABASE_URL=sqlite:///./agrosmart.db

# Optional: Model paths (if using trained models)
CROP_MODEL_PATH=models/crop_model.pkl
YIELD_MODEL_PATH=models/yield_model.pkl
